## How to use it

```bash
$ # Get the code
$ cd vending-machine
$
$ # install npm dependencies
$ npm install
$
$ # to run npm server
$ npm run dev 
$
$ # Access the fronedn in browser: http://127.0.0.1:8080/

```